import React from "react";

const UnauthorizedAccess = () => {
  // Todo: add a form
  // onSubmit run history.back()
  return (
    <div>
      <p>You are not authorized to view this page. Please log in first</p>
    </div>
  );
};

export default UnauthorizedAccess;
