import React from "react";

const ListingDoesNotExist = (props) => {
  return (
    <div>
      {/* Add some picture? */}
      <p>
        This listing does not exist. Please check that you have keyed in the
        correct address
      </p>
    </div>
  );
};

export default ListingDoesNotExist;
