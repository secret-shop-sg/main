class Listing {
  constructor(listingId, title, platform, owner, date, description) {
    this.listingId = listingId;
    this.title = title;
    this.platform = platform;
    this.owner = owner;
    this.date = date;
    this.description = description;
  }
}

export default Listing;
