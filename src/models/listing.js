class Listing {
  constructor(listingId, title, platform, owner) {
    this.listingId = listingId;
    this.title = title;
    this.platform = platform;
    this.owner = owner;
  }
}

export default Listing;
